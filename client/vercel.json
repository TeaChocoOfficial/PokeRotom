{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "installCommand": "npm i --force",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ]
}