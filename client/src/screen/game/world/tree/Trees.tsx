// -Path: "PokeRotom/client/src/screen/game/world/tree/Threes.tsx"
import useNoise from '../hooks/noise';

export default function Trees({ seed }: { seed: string }) {
    const {} = useNoise(seed);
    return <></>;
}
